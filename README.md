## Development

1. Run the redis first:
```
docker-compose up -d
```

2. Install the dependencies and develop
```
pnpm install
pnpm dev
```
open http://localhost:3000


3. Build and start
```
pnpm build
pnpm start
```
